'use client'; import {FormEvent,useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import {Input} from '@/components/ui/input'; import {Button} from '@/components/ui/button';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');const s=createClient();const{error}=await s.auth.signInWithPassword({email,password});if(error){setError(error.message);return}const{data:{user}}=await s.auth.getUser();const{data:p}=await s.from('profiles').select('role').eq('id',user?.id).single();const role=p?.role;window.location.href=role==='super_admin'?'/admin':role==='tenant_owner'?'/tenant/dashboard':role==='shop_manager'?'/shop/dashboard':role==='barber'?'/barber/dashboard':'/customer/dashboard'}return <main className="grid min-h-screen place-items-center p-6"><form onSubmit={submit} className="w-full max-w-md space-y-4 rounded-2xl bg-white p-8 shadow"><h1 className="text-3xl font-bold">BARBA10</h1><p className="text-gray-500">Acesse sua conta</p><Input type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)} required/><Input type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)} required/><Button className="w-full">Entrar</Button>{error&&<p className="text-sm text-red-600">{error}</p>}<a className="block text-center text-sm underline" href="/register">Criar conta</a></form></main>}
