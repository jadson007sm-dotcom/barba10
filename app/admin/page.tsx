import {createClient} from '@/lib/supabase/server'; import {redirect} from 'next/navigation'; import {Card} from '@/components/ui/card'; import {AppShell} from '@/components/AppShell';
export default async function Admin(){const s=createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const{data:p}=await s.from('profiles').select('role').eq('id',user.id).single();if(p?.role!=='super_admin')redirect('/login');const [{count:tenants},{count:shops},{count:appointments}]=await Promise.all([s.from('tenants').select('*',{count:'exact',head:true}),s.from('shops').select('*',{count:'exact',head:true}),s.from('appointments').select('*',{count:'exact',head:true})]);return <AppShell title="Dashboard Super Admin"><div className="grid gap-4 md:grid-cols-3"><Card><b>{tenants??0}</b><p>Tenants</p></Card><Card><b>{shops??0}</b><p>Unidades</p></Card><Card><b>{appointments??0}</b><p>Agendamentos</p></Card></div><div className="mt-6"><a className="underline" href="/admin/tenants">Gerenciar tenants →</a></div></AppShell>}
