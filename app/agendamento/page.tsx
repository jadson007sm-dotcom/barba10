"use client";

import { useMemo, useState } from "react";
import "./agendamento.css";

type Step = "date" | "time" | "barber" | "service" | "summary";
const steps:Step[]=["date","time","barber","service","summary"];
const stepTitle:Record<Step,string>={date:"Escolha a data",time:"Escolha o horário",barber:"Escolha o barbeiro",service:"Escolha os serviços",summary:"Resumo do agendamento"};
const timeSlots=["08:00","08:30","09:00","09:30","10:00","10:30","11:00","11:30","13:00","13:30","14:00","14:30","15:00","15:30","16:00","16:30","17:00","17:30","18:00","18:30"];
const barbers=["Barbeiro 1","Barbeiro 2","Barbeiro 3"];
const serviceOptions=["Corte","Barba","Corte + Barba","Sobrancelha"];
const monthNames=["Janeiro","Fevereiro","Março","Abril","Maio","Junho","Julho","Agosto","Setembro","Outubro","Novembro","Dezembro"];
function toDateKey(date:Date){return `${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,"0")}-${String(date.getDate()).padStart(2,"0")}`}
function formatDate(value:string){if(!value)return "";const [y,m,d]=value.split("-").map(Number);return new Date(y,m-1,d).toLocaleDateString("pt-BR",{weekday:"long",day:"2-digit",month:"long",year:"numeric"})}
export default function ClientBookingPage(){
 const params=typeof window!=="undefined"?new URLSearchParams(window.location.search):null;
 const barbershopName=params?.get("nome")||"Sua Barbearia";
 const [step,setStep]=useState<Step>("date");
 const [month,setMonth]=useState(new Date(new Date().getFullYear(),new Date().getMonth(),1));
 const [date,setDate]=useState("");const [time,setTime]=useState("");const [barber,setBarber]=useState("");const [services,setServices]=useState<string[]>([]);const [confirmed,setConfirmed]=useState(false);
 const today=new Date();today.setHours(0,0,0,0);
 const calendar=useMemo(()=>{const first=new Date(month.getFullYear(),month.getMonth(),1);const last=new Date(month.getFullYear(),month.getMonth()+1,0);const cells:Date[]=[];for(let i=0;i<first.getDay();i++)cells.push(new Date(month.getFullYear(),month.getMonth(),-i));for(let d=1;d<=last.getDate();d++)cells.push(new Date(month.getFullYear(),month.getMonth(),d));while(cells.length%7)cells.push(new Date(month.getFullYear(),month.getMonth()+1,cells.length-cells.length%7+1));return cells},[month]);
 const next=()=>{if(step==="date"&&date)setStep("time");else if(step==="time"&&time)setStep("barber");else if(step==="barber"&&barber)setStep("service");else if(step==="service"&&services.length)setStep("summary")};
 const back=()=>{const i=steps.indexOf(step);if(i>0)setStep(steps[i-1])};
 const toggleService=(service:string)=>setServices(current=>current.includes(service)?current.filter(x=>x!==service):[...current,service]);
 return <main className="booking-shell"><header className="booking-header"><div className="booking-brand">Barba<span>10</span></div><div className="booking-shop-name">{barbershopName}</div></header><section className="booking-content"><div className="booking-progress">{steps.map((item,i)=><span key={item} className={steps.indexOf(step)>=i?"done":""}/>)}</div><div className="booking-card"><span className="booking-kicker">AGENDAMENTO</span><h1>{confirmed?"Agendamento solicitado":stepTitle[step]}</h1><p className="booking-subtitle">{confirmed?"Seu pedido foi registrado para confirmação da barbearia.":"Agende seu atendimento de forma rápida e segura."}</p>{!confirmed&&step==="date"&&<div className="calendar"><div className="calendar-nav"><button type="button" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))} disabled={month<=new Date(today.getFullYear(),today.getMonth(),1)}>‹</button><strong>{monthNames[month.getMonth()]} {month.getFullYear()}</strong><button type="button" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))}>›</button></div><div className="weekdays">{["Dom","Seg","Ter","Qua","Qui","Sex","Sáb"].map(d=><span key={d}>{d}</span>)}</div><div className="calendar-grid">{calendar.map((d,i)=>{const current=d.getMonth()===month.getMonth();const past=d<today;const key=toDateKey(d);return <button key={`${key}-${i}`} type="button" disabled={!current||past} className={`${date===key?"selected ":""}${!current?"muted ":""}`} onClick={()=>setDate(key)}>{d.getDate()}</button>})}</div></div>}{!confirmed&&step==="time"&&<div className="choice-grid">{timeSlots.map(item=><button key={item} type="button" className={time===item?"selected":""} onClick={()=>setTime(item)}>{item}</button>)}</div>}{!confirmed&&step==="barber"&&<div className="choice-grid">{barbers.map(item=><button key={item} type="button" className={barber===item?"selected":""} onClick={()=>setBarber(item)}>{item}</button>)}</div>}{!confirmed&&step==="service"&&<div className="choice-grid">{serviceOptions.map(item=><button key={item} type="button" className={services.includes(item)?"selected":""} onClick={()=>toggleService(item)}>{services.includes(item)?"✓ ":""}{item}</button>)}</div>}{!confirmed&&step==="summary"&&<div className="summary"><div><span>Barbearia</span><strong>{barbershopName}</strong></div><div><span>Data</span><strong>{formatDate(date)}</strong></div><div><span>Horário</span><strong>{time}</strong></div><div><span>Barbeiro</span><strong>{barber}</strong></div><div><span>Serviços</span><strong>{services.join(", ")}</strong></div><button className="confirm" type="button" onClick={()=>setConfirmed(true)}>Confirmar agendamento</button></div>}{!confirmed&&<div className="booking-actions">{step!=="date"&&<button className="secondary" type="button" onClick={back}>Voltar</button>}{step!=="summary"&&<button className="primary" type="button" onClick={next} disabled={(step==="date"&&!date)||(step==="time"&&!time)||(step==="barber"&&!barber)||(step==="service"&&!services.length)}>Continuar</button>}</div>}</div></section></main>;
}
