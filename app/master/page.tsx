"use client";

import { useEffect, useMemo, useState } from "react";
import { createClient } from "@/lib/supabase";

type Barbershop = { id:string; name:string; responsible_name:string; document:string|null; whatsapp:string; email:string; cep:string; address:string; city:string; state:string; number:string; created_at:string };
type CepResponse = { logradouro?:string; localidade?:string; uf?:string; erro?:boolean };
type BarbershopForm = { name:string; responsibleName:string; document:string; whatsapp:string; email:string; cep:string; address:string; city:string; state:string; number:string; password:string; confirmPassword:string };

export default function MasterPage() {
  const [menuOpen,setMenuOpen]=useState(false),[filtersOpen,setFiltersOpen]=useState(false),[myBarbershopOpen,setMyBarbershopOpen]=useState(false),[visionOpen,setVisionOpen]=useState(false),[barbershopAuthOpen,setBarbershopAuthOpen]=useState(false),[authMode,setAuthMode]=useState<"login"|"signup">("signup"),[search,setSearch]=useState(""),[status,setStatus]=useState<"Todas"|"Ativa"|"Inativa">("Todas"),[sort,setSort]=useState<"recent"|"name">("recent"),[barbershops,setBarbershops]=useState<Barbershop[]>([]),[loadingBarbershops,setLoadingBarbershops]=useState(true),[authLoading,setAuthLoading]=useState(false),[authMessage,setAuthMessage]=useState(""),[cepLoading,setCepLoading]=useState(false);
  const [form,setForm]=useState<BarbershopForm>({name:"",responsibleName:"",document:"",whatsapp:"",email:"",cep:"",address:"",city:"",state:"",number:"",password:"",confirmPassword:""});
  useEffect(()=>{createClient().auth.getUser().then(({data})=>{if(!data.user)window.location.href="/"})},[]);
  async function loadBarbershops(){setLoadingBarbershops(true);const {data,error}=await createClient().from("barbershops").select("*").order("created_at",{ascending:false});if(!error&&data)setBarbershops(data as Barbershop[]);setLoadingBarbershops(false)}
  useEffect(()=>{loadBarbershops()},[]);
  async function logout(){await createClient().auth.signOut();window.location.href="/"}
  const filteredBarbershops=useMemo(()=>{const term=search.trim().toLocaleLowerCase("pt-BR");const filtered=barbershops.filter(b=>{const searchable=[b.name,b.city,b.state,b.address].join(" ").toLocaleLowerCase("pt-BR");return (!term||searchable.includes(term))&&(status==="Todas")});return [...filtered].sort((a,b)=>sort==="name"?a.name.localeCompare(b.name,"pt-BR"):new Date(b.created_at).getTime()-new Date(a.created_at).getTime())},[barbershops,search,sort,status]);
  const hasFilters=Boolean(search.trim())||status!=="Todas"||sort!=="recent";
  function clearFilters(){setSearch("");setStatus("Todas");setSort("recent")}
  function openBarbershops(){setMyBarbershopOpen(false);setVisionOpen(false);setBarbershopAuthOpen(false);setMenuOpen(false)}
  function openMyBarbershop(){setMyBarbershopOpen(true);setVisionOpen(false);setBarbershopAuthOpen(false);setMenuOpen(false)}
  function openBarbershopAuth(){setBarbershopAuthOpen(true);setAuthMode("signup");setAuthMessage("");setVisionOpen(false)}
  function updateField(field:keyof BarbershopForm,value:string){setForm(current=>({...current,[field]:value}))}
  function barbershopPayload(userId:string,userEmail?:string){return {owner_id:userId,name:form.name.trim(),responsible_name:form.responsibleName.trim(),document:form.document.trim()||null,whatsapp:form.whatsapp.trim(),email:form.email.trim()||userEmail||"",cep:form.cep.trim(),address:form.address.trim(),city:form.city.trim(),state:form.state.trim().toUpperCase(),number:form.number.trim()}}
  async function finishBarbershopRegistration(supabase:ReturnType<typeof createClient>,userId:string,userEmail?:string){const {data:existing,error:findError}=await supabase.from("barbershops").select("id").eq("owner_id",userId).limit(1).maybeSingle();if(findError)return {error:findError};if(existing)return {error:null};const {error}=await supabase.from("barbershops").insert(barbershopPayload(userId,userEmail));return {error}}
  async function lookupCep(){const cep=form.cep.replace(/\D/g,"");if(cep.length!==8)return;setCepLoading(true);setAuthMessage("");try{const response=await fetch(`https://viacep.com.br/ws/${cep}/json/`);const data=await response.json() as CepResponse;if(data.erro)setAuthMessage("CEP não encontrado. Confira o número informado.");else setForm(current=>({...current,cep,address:data.logradouro||current.address,city:data.localidade||current.city,state:data.uf||current.state}))}catch{setAuthMessage("Não foi possível consultar o CEP agora. Você pode preencher o endereço manualmente.")}finally{setCepLoading(false)}}
  async function submitBarbershopAuth(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setAuthLoading(true);setAuthMessage("");const supabase=createClient();
    if(authMode==="login"){const {data,error}=await supabase.auth.signInWithPassword({email:form.email.trim(),password:form.password});if(error){setAuthLoading(false);setAuthMessage("Não foi possível entrar. Confira seu e-mail e senha.");return}const {data:shop,error:shopError}=await supabase.from("barbershops").select("id").eq("owner_id",data.user.id).limit(1).maybeSingle();if(shopError){setAuthLoading(false);setAuthMessage("Login realizado, mas não foi possível verificar o cadastro da barbearia. Tente novamente.");return}if(!shop){const metadata=data.user.user_metadata as Record<string,unknown>|undefined;const savedName=typeof metadata?.barbershop_name==="string"?metadata.barbershop_name:"";const savedResponsible=typeof metadata?.barbershop_responsible_name==="string"?metadata.barbershop_responsible_name:"";const savedWhatsapp=typeof metadata?.barbershop_whatsapp==="string"?metadata.barbershop_whatsapp:"";const savedCep=typeof metadata?.barbershop_cep==="string"?metadata.barbershop_cep:"";const savedAddress=typeof metadata?.barbershop_address==="string"?metadata.barbershop_address:"";const savedCity=typeof metadata?.barbershop_city==="string"?metadata.barbershop_city:"";const savedState=typeof metadata?.barbershop_state==="string"?metadata.barbershop_state:"";const savedNumber=typeof metadata?.barbershop_number==="string"?metadata.barbershop_number:"";const savedDocument=typeof metadata?.barbershop_document==="string"?metadata.barbershop_document:"";const savedEmail=typeof metadata?.barbershop_email==="string"?metadata.barbershop_email:data.user.email||"";if(!savedName||!savedResponsible||!savedWhatsapp||!savedCep||!savedAddress||!savedCity||!savedState||!savedNumber){setAuthLoading(false);setAuthMessage("O acesso foi confirmado, mas esta conta ainda precisa concluir o cadastro da barbearia. Use a aba Cadastro, preencha os dados e clique em Cadastrar Barbearia.");setAuthMode("signup");return}const {error:insertError}=await supabase.from("barbershops").insert({owner_id:data.user.id,name:savedName,responsible_name:savedResponsible,document:savedDocument||null,whatsapp:savedWhatsapp,email:savedEmail,cep:savedCep,address:savedAddress,city:savedCity,state:savedState.toUpperCase(),number:savedNumber});if(insertError){setAuthLoading(false);setAuthMessage("Login realizado, mas não foi possível concluir o cadastro da barbearia. Tente novamente.");return}}setAuthLoading(false);window.location.href="/barbearia";return}
    if(form.password.length<6){setAuthLoading(false);setAuthMessage("A senha precisa ter pelo menos 6 caracteres.");return}if(form.password!==form.confirmPassword){setAuthLoading(false);setAuthMessage("As senhas não conferem.");return}if(!form.name||!form.responsibleName||!form.whatsapp||!form.email||!form.cep||!form.address||!form.number||!form.city||!form.state){setAuthLoading(false);setAuthMessage("Preencha todos os campos obrigatórios.");return}
    const signupMetadata={full_name:form.responsibleName.trim(),barbershop_name:form.name.trim(),barbershop_responsible_name:form.responsibleName.trim(),barbershop_document:form.document.trim(),barbershop_whatsapp:form.whatsapp.trim(),barbershop_email:form.email.trim(),barbershop_cep:form.cep.trim(),barbershop_address:form.address.trim(),barbershop_city:form.city.trim(),barbershop_state:form.state.trim().toUpperCase(),barbershop_number:form.number.trim()};
    const {data:signUpData,error:signUpError}=await supabase.auth.signUp({email:form.email.trim(),password:form.password,options:{data:signupMetadata}});
    if(!signUpError&&signUpData.user){if(!signUpData.session){setAuthLoading(false);setAuthMessage("Cadastro criado. Confirme o e-mail recebido para ativar o acesso e depois entre pela opção Login.");setAuthMode("login");return}const {error:insertError}=await finishBarbershopRegistration(supabase,signUpData.user.id,signUpData.user.email);setAuthLoading(false);if(insertError){setAuthMessage("O acesso foi criado, mas não foi possível salvar os dados da barbearia. Entre novamente para concluir.");return}window.location.href="/barbearia";return}
    const existingUserError=signUpError?.message?.toLowerCase().includes("already registered")||signUpError?.message?.toLowerCase().includes("already exists")||signUpError?.message?.toLowerCase().includes("já está cadastrado");
    if(existingUserError){const {data:loginData,error:loginError}=await supabase.auth.signInWithPassword({email:form.email.trim(),password:form.password});if(loginError||!loginData.user){setAuthLoading(false);setAuthMessage("Esta conta já existe. Use a senha dessa conta para concluir o cadastro da barbearia.");return}const {error:updateError}=await supabase.auth.updateUser({data:signupMetadata});if(updateError){setAuthLoading(false);setAuthMessage("Acesso confirmado, mas não foi possível atualizar os dados do cadastro. Tente novamente.");return}const {error:insertError}=await finishBarbershopRegistration(supabase,loginData.user.id,loginData.user.email);setAuthLoading(false);if(insertError){setAuthMessage("Os dados foram atualizados, mas não foi possível concluir o cadastro da barbearia. Tente novamente.");return}window.location.href="/barbearia";return}
    setAuthLoading(false);setAuthMessage(signUpError?.message||"Não foi possível criar o acesso da barbearia.");
  }
  return <main className="master-shell">
    <header className="master-header"><div className="master-brand" aria-label="Barba10 — Painel Master"><div className="master-logo">Barba<span>10</span></div><div className="master-slogan">Painel Master</div></div><button className="menu-button" type="button" aria-label={menuOpen?"Fechar menu":"Abrir menu"} aria-expanded={menuOpen} onClick={()=>setMenuOpen(o=>!o)}><span/><span/><span/></button></header>
    <aside className={`master-sidebar${menuOpen?" open":""}`} aria-hidden={!menuOpen}><div className="sidebar-content"><button className="sidebar-logout" type="button" onClick={openBarbershops}>Barbearias</button><button className="sidebar-logout" type="button" onClick={openMyBarbershop}>Minha Barbearia</button><button className="sidebar-logout" onClick={logout} type="button">Sair</button></div></aside>
    {barbershopAuthOpen?<section className="barbershop-auth-panel" aria-label="Login e cadastro de barbearias"><div className="barbershop-auth-card"><button className="back-link" type="button" onClick={()=>setBarbershopAuthOpen(false)}>← Voltar</button><div className="auth-heading"><span className="vision-kicker">BARBA10 • SUA BARBEARIA</span><h1>{authMode==="signup"?"Cadastre sua barbearia":"Login da barbearia"}</h1><p>{authMode==="signup"?"Crie o acesso da sua barbearia e comece a organizar seus agendamentos.":"Entre para acessar o ambiente da sua barbearia."}</p></div><div className="auth-mode-tabs"><button type="button" className={authMode==="signup"?"active":""} onClick={()=>{setAuthMode("signup");setAuthMessage("")}}>Cadastro</button><button type="button" className={authMode==="login"?"active":""} onClick={()=>{setAuthMode("login");setAuthMessage("")}}>Login</button></div><form onSubmit={submitBarbershopAuth}>{authMode==="signup"?<div className="form-grid"><label className="form-field"><span>Nome da barbearia *</span><input value={form.name} onChange={e=>updateField("name",e.target.value)} placeholder="Ex.: Barbearia Jadson"/></label><label className="form-field"><span>Nome do responsável *</span><input value={form.responsibleName} onChange={e=>updateField("responsibleName",e.target.value)} placeholder="Nome completo"/></label><label className="form-field"><span>CNPJ ou CPF (opcional)</span><input value={form.document} onChange={e=>updateField("document",e.target.value)} placeholder="Documento"/></label><label className="form-field"><span>WhatsApp *</span><input value={form.whatsapp} onChange={e=>updateField("whatsapp",e.target.value)} placeholder="(00) 00000-0000"/></label><label className="form-field full"><span>E-mail *</span><input type="email" value={form.email} onChange={e=>updateField("email",e.target.value)} placeholder="barbearia@email.com"/></label><label className="form-field"><span>CEP *</span><input inputMode="numeric" value={form.cep} onChange={e=>updateField("cep",e.target.value)} onBlur={lookupCep} placeholder="00000-000"/></label><div className="cep-action"><button type="button" onClick={lookupCep} disabled={cepLoading}>{cepLoading?"Buscando...":"Buscar CEP"}</button></div><label className="form-field full"><span>Endereço *</span><input value={form.address} onChange={e=>updateField("address",e.target.value)} placeholder="Rua, avenida..."/></label><label className="form-field"><span>Número *</span><input value={form.number} onChange={e=>updateField("number",e.target.value)} placeholder="123"/></label><label className="form-field"><span>Cidade *</span><input value={form.city} onChange={e=>updateField("city",e.target.value)} placeholder="Cidade"/></label><label className="form-field"><span>Estado *</span><input maxLength={2} value={form.state} onChange={e=>updateField("state",e.target.value.toUpperCase())} placeholder="BA"/></label><label className="form-field"><span>Senha *</span><input type="password" value={form.password} onChange={e=>updateField("password",e.target.value)} placeholder="Mínimo de 6 caracteres"/></label><label className="form-field"><span>Confirmação de senha *</span><input type="password" value={form.confirmPassword} onChange={e=>updateField("confirmPassword",e.target.value)} placeholder="Repita sua senha"/></label></div>:<div className="login-fields"><label className="form-field"><span>E-mail *</span><input type="email" value={form.email} onChange={e=>updateField("email",e.target.value)} placeholder="barbearia@email.com"/></label><label className="form-field"><span>Senha *</span><input type="password" value={form.password} onChange={e=>updateField("password",e.target.value)} placeholder="Sua senha"/></label></div>}<button className="auth-submit" type="submit" disabled={authLoading}>{authLoading?"Processando...":authMode==="signup"?"Cadastrar Barbearia":"Entrar"}</button>{authMessage&&<div className="auth-feedback" role="status">{authMessage}</div>}</form></div></section>:!visionOpen&&myBarbershopOpen?<section className="barbearias-panel" aria-label="Minha Barbearia"><div className="barbearias-header"><div><h1>Minha Barbearia</h1></div></div><div className="my-barbershop-card"><div className="my-barbershop-icon" aria-hidden="true">✂</div><h2>Minha Barbearia</h2><p>Conheça a visão do Barba10 para uma gestão mais simples, organizada e profissional da sua barbearia.</p><button className="vision-button" type="button" onClick={()=>setVisionOpen(true)}>Visão da barbearia</button></div></section>:visionOpen?<section className="barbershop-vision" aria-label="Visão da barbearia"><div className="vision-hero"><span className="vision-kicker">BARBA10 • GESTÃO INTELIGENTE</span><h1>Sua barbearia merece uma agenda que trabalhe por você.</h1><p>Uma agenda organizada é mais do que marcar horários: é controlar o seu dia, reduzir conflitos e oferecer uma experiência profissional para cada cliente.</p><button className="create-barbershop-button" type="button" onClick={openBarbershopAuth}>Criar Barbearia</button></div><div className="vision-benefits"><article><span aria-hidden="true">◷</span><h2>Organize seus agendamentos</h2><p>Tenha os horários centralizados para saber quem chega, quando chega e como está o seu dia.</p></article><article><span aria-hidden="true">✓</span><h2>Evite conflitos de horário</h2><p>Uma gestão digital ajuda a reduzir erros, encaixes desorganizados e horários esquecidos.</p></article><article><span aria-hidden="true">★</span><h2>Valorize a experiência</h2><p>Facilite a jornada do cliente e passe uma imagem de organização, confiança e profissionalismo.</p></article></div><div className="vision-footer-card"><div><h2>Mais controle para você. Mais tranquilidade para sua equipe.</h2><p>Comece sua jornada de gestão com o Barba10 e construa uma rotina mais previsível e eficiente.</p></div><button className="create-barbershop-button secondary" type="button" onClick={openBarbershopAuth}>Criar Barbearia</button></div></section>:<section className="barbearias-panel" aria-label="Barbearias"><div className="barbearias-header"><div><h1>Barbearias</h1></div><span className="barbearias-count">{filteredBarbershops.length}</span></div><div className={`barbearias-filters${filtersOpen?" expanded":""}`}><div className="compact-filter-row"><label className="smart-search"><span aria-hidden="true">⌕</span><input type="search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar barbearia..."/>{search&&<button type="button" className="clear-search" onClick={()=>setSearch("")}>×</button>}</label><button className={`filter-toggle${hasFilters?" active":""}`} type="button" onClick={()=>setFiltersOpen(o=>!o)}><span aria-hidden="true">{filtersOpen?"⌃":"☷"}</span>{filtersOpen?"Minimizar":"Filtros"}</button></div>{filtersOpen&&<div className="filter-controls"><label className="filter-select"><span>Status</span><select value={status} onChange={e=>setStatus(e.target.value as typeof status)}><option>Todas</option><option>Ativa</option><option>Inativa</option></select></label><label className="filter-select"><span>Ordenar</span><select value={sort} onChange={e=>setSort(e.target.value as typeof sort)}><option value="recent">Mais recentes</option><option value="name">Nome A–Z</option></select></label>{hasFilters&&<button className="clear-filters" type="button" onClick={clearFilters}>Limpar filtros</button>}</div>}</div>{loadingBarbershops?<div className="barbearias-empty-filter"><h2>Carregando barbearias...</h2></div>:filteredBarbershops.length===0?<div className="barbearias-empty-filter"><div className="empty-filter-icon" aria-hidden="true">✂</div><h2>{hasFilters?"Nenhum resultado encontrado":"Aguardando barbearias"}</h2><p>{hasFilters?"Ajuste a busca ou os filtros para encontrar uma barbearia.":"Quando uma barbearia for cadastrada, ela aparecerá aqui automaticamente."}</p></div>:<div className="barbearias-grid">{filteredBarbershops.map(b=><article className="barbearia-card" key={b.id}><div><h2>{b.name}</h2><p>Número: {b.number} • {b.city} - {b.state}</p></div><span className="status-badge ativa">Ativa</span></article>)}</div>}</section>}
  </main>;
}
