import {ReactNode} from 'react'; import {AppShell} from './AppShell'; import {Card} from './ui/card'; export function RolePage({title,links,children}:{title:string;links:string[];children?:ReactNode}){return <AppShell title={title}><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{links.map(x=><a key={x} href={x.split('|')[1]||'#'}><Card>{x.split('|')[0]}</Card></a>)}</div>{children}</AppShell>}
