'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; import {Input} from '@/components/ui/input'; import {Button} from '@/components/ui/button'; export function TenantForm(){const[n,setN]=useState(''),[slug,setSlug]=useState('');return <div className="space-y-3"><Input placeholder="Nome" value={n} onChange={e=>setN(e.target.value)}/><Input placeholder="Slug" value={slug} onChange={e=>setSlug(e.target.value)}/><Button onClick={async()=>{const{error}=await createClient().from('tenants').insert({name:n,slug});if(error)alert(error.message);else location.href='/admin/tenants'}}>Salvar</Button></div>}
