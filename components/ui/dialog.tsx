'use client'; import {ReactNode,useState} from 'react'; export function Dialog({trigger,children}:{trigger:ReactNode;children:ReactNode}){const[o,s]=useState(false);return <>{<span onClick={()=>s(true)}>{trigger}</span>}{o&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-6">{children}<button className="mt-4 text-sm underline" onClick={()=>s(false)}>Fechar</button></div></div>}</>}
